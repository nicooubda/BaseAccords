{% extends 'base.html.twig' %}

{% block title "Gerer les types de documents" %}

{% block body %}
	<div class ='container mt-4'>
		<h2> Enregistrer sous type de document</h2>
        
        <hr class="my-7"><hr class="my-7">

        {% if exist == true %}
         <div class="alert alert-danger" role="alert">
         Le sous type de document existe déjà 
         </div>
        {% endif %}
	{{ form_start(form)}}
	<div class='row'>
		<div class="col-md-3"> {{ form_row(form.sousType)}} </div>
        <div class="col-md-3"> {{ form_row(form.type)}} </div>
    {{ form_widget(form)}}
    </div>	
        <button class="btn btn-primary"> Enregistrer </button>
    {{ form_end(form)}}
	
	<div class='text-right' >
        
    </div>
</div>

{% endblock %}