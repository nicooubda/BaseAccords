{% extends 'base.html.twig' %}

{% block title %}Hello SecurityController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    <div class ='container mt-8'>
		<h3> Inscrire un utilisateur</h3>
        <hr class="my-7">
	{{ form_start(form)}}
	{{form_row(form.nom,{'label':'Nom'})}}
	{{form_row(form.prenom,{'label':'Prenom'})}}
    {{form_row(form.email,{'label':'Adresse email'})}}	
    {{form_row(form.username,{'label':'Nom d\'utilisateur'})}}  
    {{form_row(form.password,{'label':'Mot de passe'})}} 
    {{form_row(form.confirm_password,{'label':'Confirmez le mot passe'})}} 
    {{ form_widget(form)}}

        <button class="btn btn-success"> Inscription ✅ </button>
    {{ form_end(form)}}
<hr class="my-7">
<hr class="my-7">
<hr class="my-7">
</div>

   
{% endblock %}
